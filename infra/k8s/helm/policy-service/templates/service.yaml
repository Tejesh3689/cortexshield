
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-policy-service
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: policy-service
