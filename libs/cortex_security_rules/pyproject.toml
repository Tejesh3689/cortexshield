[project]
name = "cortex_security_rules"
version = "0.1.0"
description = "CortexShield Security Rules"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["cortex_security_rules*"]
