[project]
name = "proxy-engine"
version = "0.1.0"
description = "CortexShield Proxy Engine"
requires-python = ">=3.12"
dependencies = [
    "healing-worker",
    "cortex_schemas",
]

[tool.uv.sources]
healing-worker = { workspace = true }
cortex_schemas = { workspace = true }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
